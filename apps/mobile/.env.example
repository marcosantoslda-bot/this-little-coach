# URL da API (NestJS). No telemóvel usa o IP da máquina na rede local, não localhost.
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000

# Supabase (Project Settings → API). A app só usa a chave anónima (pública).
EXPO_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
